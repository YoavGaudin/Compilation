// "Hello"
() find tokens else than
a=3
"a=3"
write("a=2") //Blah
char* ThisIsDummyCode {
	token[i] = 1;
	return token[0];
}

void invalidOp{
	
	
	
	
	// a lot of enters
	"//lets confuse the parser"
	"(more trials {} [] , ; :"
	""
	"(more trials ""
	
}
