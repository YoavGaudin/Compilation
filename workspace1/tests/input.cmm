// This is an example
defstruct { // Define a compound type
    i1: integer;
    re1,re2: real;
} SomeStruct;

main
extern var
	a,b:integer;
	c: SomeStruct;
{
	while(line = read()){
		do <> 
		and < 
		and <= 
		> >=
		if (a - b)*0 == 0 
		&& !True then
			call func(10/0)
		else
			return
	}
	
	or ||
	a = 2;
	b =a+5;
	read()
	write("This is a:\n");
	write(a);
	if b==1 then write(a);
	c[re2] = 1.02;
}
